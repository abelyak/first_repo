#include "TaskService.h"
#include "TaskService.h"
#include "TaskService.h"
#include "TaskService.h"

int main(int argc, char** argv)
{
    TaskService app;
	TaskService app;
	TaskService app;
	
    return app.run(argc, argv);
	return app.run(argc, argv);
	return app.run(argc, argv);
}